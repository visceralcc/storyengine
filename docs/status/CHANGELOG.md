# Story Engine — Changelog

All notable changes, logged per session. Tags: `[ui]` `[data]` `[infra]` `[spec]` `[fix]` `[docs]`

---

## 2026-05-21

- [spec] Completed `Spec_Development_Design.md` v0.2 — reconciled against four updated Figma screens: Development Canvas (69:53), Story Element Small component (284:284), Detail View (290:910), and Compare View (290:619). Major changes from v0.1: body font corrected to Aleo throughout; column backgrounds changed from flat #F5F5F5 to gradient (#EEEDED → #E8E8E8); card corner radius corrected from 8px to 10px; added ui_eval component (Core/Evolve/Set Aside creative tag bar with colors #9CCBAC green, #F2BA03 yellow, #AA5959 red); added Detail View with IDEA + IDEA THOUGHT / DEFINITION sections on light blue #E4F5FF backgrounds inside white inner panel; added Compare View with side-by-side elements; added comparison mode button with active/inactive SVG states; added dismiss navigation via circle × button; character cards standardized to bullet-point format; phase header typography updated (Aleo Regular 36pt for number, Barlow Thin 36pt for name, Aleo Bold 20pt #737373 for subtitle); pillar reassignment via tappable header dropdown in Detail View; added `definition` and `creativeTag` fields to Concept entity (§6.1).
- [spec] Resolved 6 of 7 open questions: Q1 (definition storage → `definition` field on Concept), Q2 (Compare View entry → comparison mode button on canvas), Q3 (manual connections → created via comparison mode selection), Q4 (element title editing → pencil icon in IDEA section), Q5 (canvas card content → shows Discovery summary only, tap to see full definition), Q6 (pillar reassignment → tappable pillar header dropdown). Q7 (Refinement phase unlock trigger) remains open.
- [docs] Updated BUILD_STATUS.md — Level 2 spec count now 10/15, Development_Design v0.2 added to inventory and writing order, 8 new key decisions logged, next step updated.
- [data] Added `CreativeTag` type (`CORE | EVOLVE | SET_ASIDE`) and `definition: string | null` + `creativeTag: CreativeTag` fields to the `Concept` interface (`src/models/types.ts`) per Spec_Development_Design §6.1. `createConcept` now accepts optional `definition` / `creativeTag`, defaulting them to `null` / `'CORE'`. All 193 tests still pass.
- [ui] Development UI Phase 1 — Development Canvas at `app/project/[projectId]/development.tsx`. Phase header ("2" Aleo Regular 36pt + "Development" Barlow Thin 36pt + Aleo Bold 20pt #737373 subtitle + 30px avatar + comparison-mode button). 309px canvas-level chat panel with gradient background (#F6F6F6→#E8E8E8), "Assistant" label, and circular send button. Three independently-scrolling gradient columns (Theme / World / Character, #EEEDED→#E8E8E8, 10px radius) each with an SVG icon header. `story_element_small` card: white 10px-radius card with a pillar-icon header label ("Pillar / Type", or "Character / [Name]"), paragraph body for Theme/World or bullet body for Character, and a 17×72 ui_eval bar on the right edge colored by Core/Evolve/Set Aside (#9CCBAC / #F2BA03 / #AA5959). Comparison-mode button toggles its active/inactive SVG. Phase 1 scope: chat panel is local-only (no AI / no persistence); tapping a card and the compare flow are Phase 2/3 stubs.
- [ui] Added `src/development/storyElements.ts` — the canvas-facing `StoryElement` view model and `getStoryElements(projectFile)`, which maps real Concepts when present and otherwise falls back to a Ready Player One sample dataset (Development_Screen_Brief §5) so the canvas is populated before the consolidation pipeline exists.
- [ui] Unlocked the "Development" row on the Step Menu (`app/project/[projectId]/steps.tsx`) to route to `/project/:projectId/development`. Loaded `Barlow_400Regular` in `app/_layout.tsx` for the spec's Barlow Regular chat label.
- [ui] Development UI Phase 2 — Story Element Detail View (`app/project/[projectId]/development.tsx`). Tapping a canvas card opens a focused three-region Detail View via a dissolve transition (`Animated` opacity: fade out → swap surface → fade in); the circle × button dissolves back to the canvas. Left region: the shared chat panel, with an opening message contextual to the active element. Center: the Writing Area — a gradient outer container with a tappable pillar header (opens a Theme/World/Character reassignment dropdown) and the × dismiss button, wrapping a white inner panel with a left-edge ui_eval bar. Inside: the editable element title, an IDEA section and an IDEA THOUGHT / DEFINITION section (light-blue #E4F5FF, Barlow Regular 15pt uppercase labels, pencil edit icons, Aleo Regular 20pt body). The IDEA pencil toggles edit mode for the title + Discovery summary; the DEFINITION field is a plain-text area that auto-grows with content and commits on blur, with the "Expand on this idea..." placeholder. Right region: the Related Elements panel — "This [Pillar] connects to:" header, drop-shadowed `story_element_small` glance cards that navigate to the connected element's Detail View, and the no-connections placeholder. Element edits (title, summary, definition, pillar) are held in workspace state — in-memory only; persistence is a later phase.
- [ui] Added `assets/icons/icon_pencil.svg` — a pencil edit icon for the IDEA / DEFINITION section headers. The spec calls for a pencil icon but none was in the provided asset set; this is a stand-in to swap for the Figma export.
- [ui] Development UI Phase 3 — Compare View + comparison-mode flow (`app/project/[projectId]/development.tsx`). The canvas comparison-mode button now toggles a selection mode (Spec §5.1): while active, tapping a story element card selects it (blue #00A5F2 highlight ring) instead of opening the Detail View, and tapping the button again cancels. Selecting a second card opens the Compare View — the two elements side by side, each in its own gradient writing area with an independent pillar header, ui_eval bar, and editable IDEA + DEFINITION sections; the shared chat panel stays on the left and the right element occupies the Related Elements panel's space (Spec §3.3). Per §4.6 only the right element shows the × button — dismissing it returns to the left element's Detail View (Related panel reappears; its own × returns to the canvas). Selecting two elements also records a two-way connection between them (Spec §4.5): `relatedIds` is updated on both, so each then appears in the other's Related Elements panel. The workspace view state is now a `DevView` union (canvas / detail / compare); every surface swap uses the dissolve transition.
- [docs] Synced project documents to the Development UI work and the data-model change (per the CLAUDE.md doc list). `Spec_DataModel.md` → **v0.4**: added the `CreativeTag` enum (`CORE | EVOLVE | SET_ASIDE`) and the `definition` / `creativeTag` fields to the Concept entity (§3, §8, §15, §17, §18) per `Spec_Development_Design.md` §6.1. `DESIGN.md`: registered the Development-phase colors (ui_eval Core/Evolve/Set Aside, comparison accent, IDEA-section blue, surface gradient stops) in the token table, recorded the subtle surface gradients as an allowed pattern, added a Development Surfaces component note, and corrected stale "Domine" body-font references to Aleo. `OVERVIEW.md`: refreshed the Current Status section (Chat Engine + Development UI Phases 1–3 complete, 193 tests). `BUILD_STATUS.md` and `Structure_Map.md`: DataModel version references bumped to v0.4 and the pending data-model roll-up reworded. `HARD_RULES.md`, the PRD, and `Templates_SpecDocs.md` needed no changes.
- [infra] Added `.env` at the project root (already covered by `.gitignore`) holding an empty `EXPO_PUBLIC_ANTHROPIC_API_KEY=` for the user's Anthropic API key — read by `loadChatEngineConfig` at runtime.
- [ui] Wired the Discovery chat panel to the Anthropic API via the Chat Engine (`app/project/[projectId]/discovery.tsx`). On send the panel appends the user message, shows a live streaming assistant bubble, and runs the pipeline — `loadChatEngineConfig` → `createChatClient` (both `useMemo`'d once per panel) → `buildDiscoveryContext` → `formatDiscoveryContextBlock` → `assembleSystemPrompt` → `selectConversationHistory` → `client.sendMessage`. Delta events fill the streaming bubble (the trailing ```json block is hidden mid-stream per §2.3); on `done` the response is parsed with `parseDiscoveryResponse`, extracted notes are placed via `extractDiscoveryNotes` inside the live canvas viewport, and the assistant `ChatMessage` is appended (persisted by the existing save watcher). Errors render as a transient italic system bubble (not persisted, §2.2); `CANCELLED` is silent. The send button disables while streaming; a new send aborts the in-flight stream via `AbortController` and discards the partial (§2.3). The `Canvas` now writes a shared `viewportRef` (`{x:-pan.x, y:-pan.y, width, height}`) on every pan so chat-extracted notes land in the visible region.
- [fix] `selectConversationHistory` (`src/engine/chat/context.ts`) now drops leading assistant messages from the returned API history. The Anthropic Messages API requires `messages[0].role === 'user'`, but the Development opening message (§9.1) is assistant-role and chronologically first — so the first Development send would have produced an invalid request. Leading assistant messages are stripped after phase-filtering and the 40-message cap; they remain in the persisted history and in the system prompt's project context. Added tests in `context.test.ts` (realistic `[assistant, user, assistant]` → `[user, assistant]`; solo assistant → `[]`) and updated the Phase 6 integration tests in `openingMessage.test.ts` to expect the opening message dropped. 196 tests pass.
- [ui] Wired the Development chat panel to the Anthropic API (`app/project/[projectId]/development.tsx`). Chat state was lifted to `DevelopmentWorkspace` — `messages`, streaming, error notice, and pending RETHINKs are workspace-level, so the conversation is continuous across the Canvas / Detail / Compare surfaces (§9.3); the engine client and `AbortController` live in refs that survive surface swaps. The whole `ProjectFile` is held in workspace state (brief Option a) so each extraction's context sees the latest concepts/types. On first entry the workspace seeds and persists an opening message via `generateOpeningMessage` keyed on creative gravity (§9.1). On send: `buildDevelopmentContext` (active dimension hardcoded to CHARACTER) → `formatDevelopmentContextBlock` → `assembleSystemPrompt` → `selectConversationHistory` → stream; on `done`, `parseExtractionResponse` + `applyConceptExtraction` create/refine concepts and concept types, the updated `ProjectFile` is persisted via `saveProject`, and `getStoryElements` re-derives the canvas cards. RETHINK proposals render as inline confirmation cards in chat ("New version" → `applyRethink`, "Update existing" → `applyRefine`); unconfirmed RETHINKs are discarded on the next send (§6.1). `ChatPanel` is now a pure display component (props: `welcome?`, `messages`, `onSend`, `isStreaming`, `streamingText`, `notice`, `pendingRethinks`, `onConfirmRethink`, `onRejectRethink`). tsc clean; 193/193 tests still pass.

## 2026-05-20

- [docs] Created `docs/design/Development_Screen_Brief.md` — pre-spec design brief capturing conceptual decisions for the Development screen: three-level hierarchy (dimension → element → detail), focus model (one dimension at a time, others recede), Core/Evolve/Set Aside element tagging, contextual relationship display, and Ready Player One example content in mid-development voice for Figma comps.
- [docs] Key decisions made: "story elements" as the user-facing mental model for Concepts; focus model over dashboard model; Core/Evolve/Set Aside as three-state creative curation tag (inspired by Continue/Pivot/Stop but softened for creative context); relationships shown contextually when focused on an element, not as a global web.
- [docs] Updated BUILD_STATUS.md — new decisions logged, Development_Screen_Brief added to inventory, next step updated to Figma design.

## 2026-05-18

- [infra] Chat Engine Phase 7 — Refinement chat integration. The Refinement system prompt (§4.3 — STORYLINE EXTRACTION listing the 8 Storyline types, EDITORIAL VOICE shift, TONE) shipped in Phase 2's `prompts.ts`, and `buildDevelopmentContext` with `phase: 'REFINEMENT'` already widens the available ConceptTypes to include STORYLINE alongside the active dimension. Phase 7 adds end-to-end verification: new `__tests__/refinementChat.test.ts` proves (a) the eight default Storyline ConceptTypes from `defaults.ts` are surfaced by the Refinement context and excluded from Development, (b) a parsed Refinement AI response extracting Storyline concepts (Story Arc, Tone, Narrative POV) lands on the correct ConceptType IDs with `dimension: STORYLINE`, (c) Refinement-phase REFINE updates to Character concepts work (§4.3 rule 4: "Continue to refine World, Character, and Conflict concepts as needed"), (d) the assembled Refinement system prompt has all editorial-voice markers AND the Storyline context block, and (e) the Refinement prompt is meaningfully different from the Development prompt (no copy-paste). 10 new tests — 189 tests total passing. **Chat Engine build sequence Phases 1–7 complete; the engine pipeline is end-to-end ready.**
- [infra] Chat Engine Phase 6 — gap-aware conversation. The gap-analysis wiring already shipped with Phase 2's `buildDevelopmentContext` + `formatDevelopmentContextBlock` and the §9.2 GAP EXPLORATION instructions already live in Phase 2's Development prompt. Phase 6 added the opening-message generator and end-to-end verification. New `src/engine/chat/openingMessage.ts` with `openingMessageText(gravity)` (returns the four §9.1 strings verbatim, with STORYLINE / unexpected falling back to the mixed-bag message) and `generateOpeningMessage({ projectId, creativeGravity, now? })` (produces a persistable assistant ChatMessage on the DEVELOPMENT phase via the existing `createChatMessage` factory). Pure function — caller (UI) owns the first-entry check per §9.1 "generated once". Added `__tests__/openingMessage.test.ts` (each gravity case + the synthetic STORYLINE fallback + integration with `selectConversationHistory` proving phase scoping per DataModel §11) and `__tests__/gapAware.test.ts` (full pipeline integration: gap analysis → context → assembled system prompt carries the GAP EXPLORATION rules AND the concrete Strong/Partial/Unrepresented bucket data; gracefully handles pre-consolidation state where `gapAnalysis: null`). 13 new tests — 179 tests total passing.
- [infra] Chat Engine Phase 5 — custom ConceptType creation. AI-initiated path was already covered by Phase 4's `applyConceptExtraction` (suggested types created first, case-insensitive duplicate skip, immediately available for same-response concepts). Phase 5 adds the user-initiated path (§7.2) by extending the Development prompt in `prompts.ts` with a CUSTOM CONCEPT TYPES section: explicit instructions to route "Create a Concept Type called X" requests through `suggestedNewTypes`, Title Case label requirement per HARD_RULES, near-duplicate guardrail (don't create "Clothing Style" when "Fashion Style" exists), confirmation-in-chat for user-initiated creation, and the rule that newly suggested types are immediately usable in the same response. Added `__tests__/conceptTypes.test.ts` exercising the end-to-end user-initiated flow plus prompt-content assertions so the §7 guardrails can't silently drift. 8 new tests — 166 tests total passing.
- [infra] Chat Engine Phase 4 — Development chat integration. Extended `parser.ts` with `parseExtractionResponse` for the §5.2 shape (`concepts`, `updatedConcepts`, `suggestedNewTypes` — any/all may be omitted). Schema-level validation only: drops entries with missing required fields, normalizes dimension casing to uppercase, defaults invalid `editType` to `REFINE` per §5.3, falls back to plain chat with `parseError` set on malformed JSON or non-object root. Added `src/engine/chat/refinement.ts` with `applyRefine` (in-place edit of current ConceptVersion: bumps `value` + `updatedAt` + `sourceMessageId`; no-op on corrupt `currentVersionId`) and `applyRethink` (creates a new ConceptVersion with `versionNumber = max + 1` and points `currentVersionId` at it; preserves prior versions per DataModel §11). Added `applyConceptExtraction` to `extraction.ts` — pure function applying §5.3 + §5.4 + §6.1 against project state: creates suggested ConceptTypes first (with case-insensitive duplicate check) so same-response concepts can reference them, creates new Concepts (case-insensitive label match, dimension correction with warning, threads `sourceMessageId`), applies `REFINE` updates immediately and re-resolves later updates against the new state, surfaces `RETHINK` proposals as pending confirmations without mutating the Concept. Returns `{ newConceptTypes, newConcepts, refinedConcepts, pendingRethinks, affectedConceptIds, warnings }`; never mutates caller-supplied arrays. 30 new unit tests — 158 tests total passing.
- [infra] Chat Engine Phase 3 — Discovery chat integration. Added `src/engine/chat/parser.ts` with `extractJsonBlock` (regex splits trailing ```` ```json ``` ```` block from chat text; takes the last block when multiple appear; rejects plain ``` blocks without a json language tag) and `parseDiscoveryResponse` (returns `{ chatResponse, notes, parseError }` — never throws). Behavior per §5.1 + §10: no JSON block → brainstorming (zero notes); valid JSON → parsed notes + text-before-block; JSON-only response → synthetic `"I've captured those ideas."` chat message; malformed JSON or schema mismatch → entire response treated as plain chat with `parseError` set for caller logging; non-string and whitespace-only entries filtered. Added `src/engine/chat/extraction.ts` with `extractDiscoveryNotes` — random 140×140 placement inside the supplied `ViewportBounds` per Spec_DiscoveryEngine §2.2, collision avoidance with up to 10 retries and last-attempt fallback, within-batch collision detection so notes scatter rather than cluster, injectable RNG + clock, viewport-smaller-than-note pinned to origin. 20 new unit tests — 128 tests total passing.
- [infra] Chat Engine Phase 2 — context assembly. Added `src/engine/chat/prompts.ts` with `BASE_IDENTITY` constant and `buildPhasePrompt(phase, { activeDimension })` covering Discovery / Development / Refinement (Production falls back to Refinement per §4.4). Each phase prompt embeds its §5.1 / §5.2 response-format protocol so the Phase 3 parser can rely on the model emitting fenced JSON. Added `src/engine/chat/context.ts` with `buildDiscoveryContext` (50-note cap, newest-first), `buildDevelopmentContext` (concept summarization that hides versions/IDs, ConceptType filter that defaults to active dimension for Development and widens to active + STORYLINE for Refinement, gap-analysis pass-through), `formatDiscoveryContextBlock` / `formatDevelopmentContextBlock` (project context blocks injected into the system prompt — discoverable structure with "Plus N earlier notes" overflow line and Strong/Partial/Unrepresented gap buckets using DEFAULT_CONCEPT_TYPES labels), `assembleSystemPrompt` (layers Base Identity → phase prompt → project context), and `selectConversationHistory` (filters by phase per DataModel §11, sorts chronologically, caps at 40 messages). 27 new unit tests — 108 tests total passing.
- [infra] Chat Engine Phase 1 — API client. Installed `@anthropic-ai/sdk@^0.96`. Added `src/engine/chat/types.ts` (ChatEngineConfig, SendMessageInput, ChatStreamEvent union, ChatEngineError with kinds `NETWORK | API | CANCELLED | EMPTY`) and `src/engine/chat/client.ts` (`createChatClient(config, sdk?)` returning an async-generator `sendMessage` that streams `text_delta` events as `'delta'` events and emits a final `'done'` event with the assembled text). API key loaded from `EXPO_PUBLIC_ANTHROPIC_API_KEY` (Expo's client-side env pattern, with `ANTHROPIC_API_KEY` fallback) via `loadChatEngineConfig`. AbortController forwarded to the SDK; `APIUserAbortError` / DOM `AbortError` → CANCELLED, `APIConnectionError` → NETWORK, `APIError` → API (429 gets dedicated rate-limit copy), no-text streams → EMPTY (§10). 17 new unit tests with an injected `AnthropicLike` fake — 81 tests total passing.
- [data] Added NoteColor type (`BLUE | GREEN | PURPLE | GOLD | PINK | GRAY`) and `color` field to DiscoveryNote interface in `src/models/types.ts`. Created `src/models/noteColors.ts` with `NOTE_COLOR_HEX` map and `DEFAULT_NOTE_COLOR`. Updated `createDiscoveryNote` factory and `canvasManager.createNote` to accept color parameter (default BLUE). 51 tests passing.
- [ui] Discovery UI Phase 1 — screen shell at `app/project/[projectId]/discovery.tsx`: phase header ("1" Noticia Text Regular 36pt + "Discovery" Barlow Thin 36pt + subtitle in Noticia Text Bold 20pt #AFAFAF + 30px circular avatar with hardcoded "C" initial), 309px chat panel placeholder (#F5F5F5, 10px radius), 1px #E8E8E8 divider, white canvas area.
- [infra] Loaded additional font weights in `app/_layout.tsx`: Noticia Text Regular, Bold, Italic; Barlow Medium.
- [ui] Discovery UI Phase 2 — chat panel rendering: scrollable AI/user message area with initial "How can I help?" assistant bubble, ~198px multi-line text input with circular send button (26×26, 1px #656363 border), Enter sends / Shift+Enter newline. Messages stored in local state — no AI integration yet.
- [ui] Discovery UI Phase 3 — note color picker: 26×26 circular toggle (using `assets/buttons/button_note_active.svg` and `button_note_inactive.svg`) sits between chat panel and canvas. Inactive on load with swatches hidden. Tap → switches to active SVG, reveals 6 stacked color swatches (BLUE/GREEN/PURPLE/GOLD/PINK/GRAY, 32×32px each, 2px radius), and sets `placementActive = true`. Selected color shows a dark 1.5px ring inset 4px around the swatch. Default selection is BLUE. The toggle is the single entry/exit for placement mode — no separate "Add Note" affordance.
- [ui] Discovery UI Phase 4 — canvas + note placement: pannable white canvas with `overflow: hidden` and an inner "world" View (transform: translateX/Y). Pointer-down on empty backdrop pans when placement is inactive (uses `setPointerCapture` so drag survives leaving the canvas). When placement is active, the canvas shows a 140×140 semi-transparent ghost preview in the selected color following the cursor; click places a note centered on the click in world coordinates and immediately enters edit mode. DiscoveryNoteCard: 140×140, 6px radius, colored bg, 8px padding, Domine_400Regular 14pt. Tap-to-edit (autofocuses TextInput); drag after 150ms or 5px (lifts via scale 1.02 + shadow + zIndex, commits via `canvasManager.updateNotePosition` on release); × on hover deletes via `canvasManager.deleteNote` (stops propagation so it doesn't start a parent drag). Enter / Escape / blur end edit; empty notes auto-delete via `canvasManager.isNoteEmpty`.
- [ui] Discovery UI Phase 5 — Consolidate Ideas button: 48px tall, full chat-panel width (309px), pill-shaped (71px border radius), `#D2D2D2` bg with `#B8B8B8` border, Domine_400Regular 16pt dark text. Wraps the chat panel and button in a vertical `leftColumn` so the chat panel grows (`flex: 1`) and the button pins below. Disabled when notes < 3 (opacity 0.45) — tapping shows a hint "Add a few more ideas first — you need at least 3 notes." that fades after 3s and clears immediately if note count reaches 3. Enabled when notes ≥ 3 (full contrast, hover darkens to `#C4C4C4`). On tap when enabled: console-logs the stub and enters a loading state for ~2.5s with the label changing to "Consolidating..." (UI stub only — no real consolidation engine in v1).
- [ui] Discovery UI Phase 6 — polish: wired trackpad two-finger pan via `onWheel` on the canvas backdrop (wheel deltaX/Y subtract from pan offset, with `preventDefault` to suppress page-scroll fallback). Verified end-to-end: tsc clean, 51/51 tests pass, all four entry-flow routes (`/`, `/choose`, `/project/:id/steps`, `/project/:id/discovery`) load without Metro errors.
- [docs] Updated BUILD_STATUS.md and CHANGELOG.md for full Discovery UI build (Phases 1–6).
- [data] DataPersistence Phase 2 — project lifecycle. Added `src/persistence/storage.ts` with a `KvStorage` abstraction and two backends: `createMemoryStorage()` (tests) and `createLocalStorageStorage()` (browser, lazily resolved so module load is safe under jest). Added `src/persistence/projectStore.ts` with `saveProject` / `loadProject` / `listProjects` / `deleteProject`, `ProjectNotFoundError`, `ProjectCorruptError`, and a `getDefaultProjectStore()` singleton. Added `createInitialProjectFile` factory and `CURRENT_SCHEMA_VERSION = 2` to `src/models/factories.ts`. 13 new tests cover the round trip, summary sort by `updatedAt` desc, corrupt-entry skip, not-found / corrupt errors, and idempotent delete.
- [ui] Wired Project Chooser (`app/choose.tsx`) — "Start New Story" now builds a full ProjectFile via `createInitialProjectFile`, writes it via `saveProject`, and then navigates. "Open existing Story" swaps the two-row menu for an inline list (sorted newest first) as a placeholder for the spec's dedicated Project List screen.
- [ui] Wired Discovery screen (`app/project/[projectId]/discovery.tsx`) — split into a thin loader (`DiscoveryRoute`) and a `DiscoveryWorkspace` that initializes notes, clusters, and chat messages from the loaded bundle. A single effect watching `[notes, clusters, messages]` writes the full bundle back to disk on every change (Phase 3 will add debounce). Chat messages now use the model `ChatMessage` shape via `createChatMessage`; the "How can I help?" welcome bubble is rendered as a static UI affordance, not part of persisted history.
- [spec] Phase 2 departures from `Spec_DataPersistence.md` for v1: one localStorage key per project instead of a per-project folder, no `.bak` (the existing `atomicWrite.ts` is retained for the future local server in Phase 5), and no save queue / debounce (Phase 3).
- [docs] BUILD_STATUS.md: DataPersistence Phase 2 marked complete; tests = 64 passing (20 model, 16 canvas, 28 persistence).

## 2026-05-17

- [ui] Built entry flow UI — Phases 2–5 complete. Four commits on main:
  - Phase 2: Splash screen (`app/index.tsx`) — "STORYENGINE" in Barlow Thin, responsive font size min(260pt, 14vw), white→dark background transition over 500ms simulating video load, tap-anywhere or any-keypress navigation to /choose. Uses Animated API for color transitions (useNativeDriver: false).
  - Phase 3: Project Chooser (`app/choose.tsx`) — two rows ("Open existing Story" / "Start New Story") with Barlow Thin 40pt labels and Noticia Text Bold Italic 24pt annotations ("past" / "prologue"). 922px horizontal rules. Upper-left quadrant composition with percentage-based positioning.
  - Phase 4: Step Menu (`app/project/[projectId]/steps.tsx`) — three phase rows (Discovery / Development / Refinement) in Barlow Thin 96pt. Locked/unlocked visuals (#1A1A1A vs #999999 text, dark vs #D2D2D2 rules). Only Discovery tappable; locked rows use View instead of Pressable.
  - Phase 5: Polish — "Start New Story" now calls `initializeProject({ name: 'Untitled Story' })` for real project IDs. Hover states (opacity 0.7) on tappable rows via onHoverIn/onHoverOut.
- [infra] Installed expo-font, @expo-google-fonts/barlow, @expo-google-fonts/noticia-text.
- [ui] Font loading centralized in `app/_layout.tsx` — Barlow_100Thin and NoticiaText_700Bold_Italic loaded via useFonts, returns null until ready.
- [docs] Updated BUILD_STATUS.md — entry flow implementation complete, Discovery UI marked as next.
- [docs] Created docs/handoffs/ folder with EntryFlow_Phases2-5_Handoff.md and Discovery_Screen_Handoff.md.

## 2026-05-14

- [infra] Scaffolded Expo SDK 52 + TypeScript + Jest (jest-expo preset). Added nanoid to transformIgnorePatterns so its CJS build resolves under Jest.
- [data] Built DataModel Phase 1 — `src/models/types.ts` (all enums, entity interfaces, GapAnalysis additions from Discovery Engine §10), `src/models/defaults.ts` (41 default ConceptTypes: 11 World + 13 Character + 9 Conflict + 8 Storyline), `src/models/factories.ts` (factories for every entity, `initializeProject` bundle, atomic `createConcept` with first ConceptVersion). 19 unit tests passing.
- [data] Built Discovery Engine Phase 1 — `src/engine/discovery/canvasManager.ts` with `createNote`, `updateNoteContent`, `updateNotePosition`, `isNoteEmpty`, `findNote`, `deleteNote` (drops empty clusters per §4.5). All functions pure; 16 unit tests passing.
- [data] Built DataPersistence Phase 1 — `src/persistence/paths.ts` (StoryEngine folder-layout helpers with overridable root for tests), `src/persistence/atomicWrite.ts` (tmp→bak→rename atomic write, `readWithRecovery` + `readJsonWithRecovery` with .bak fallback and stale-`.tmp` cleanup, `UnrecoverableReadError` when both primary and .bak fail). 15 unit tests passing (40 total across persistence + models + discovery).
- [spec] Completed Spec_Discovery_Design.md v0.1 — Discovery canvas layout, note interaction, 6-color note picker, chat panel ("Assistant"), consolidation states, post-consolidation cluster view, creative gravity indicator. Based on Figma designs (Screen_Discovery 46:34).
- [spec] Design decision: Note colors added to v1. 6-color palette (Blue, Green, Purple, Gold, Pink, Gray). NoteColor type and color field on DiscoveryNote pending DataModel v0.3.
- [spec] Design decision: Body font changed from Noto Serif to Noticia Text. Pending update to HARD_RULES.md and DESIGN.md.
- [spec] Design decision: Chat panel labeled "Assistant" across all phases (placeholder — can change later).
- [docs] Updated BUILD_STATUS.md — Level 2 spec count now 5/13, Phase B Discovery specs complete, new decisions logged.

## 2026-05-13

- [docs] Initialized project documentation structure (CLAUDE.md, CHANGELOG.md, DESIGN.md)
- [docs] Reorganized folder structure: status/, design/ subfolders, specs moved to foundation/
- [docs] Replaced outdated v0.1 foundation specs with v0.2 versions

## 2026-05-12

- [spec] Updated BUILD_STATUS.md, HARD_RULES.md, OVERVIEW.md for PRD v0.3 pipeline model
- [spec] Revised Spec_DataModel.md to v0.2 — added DiscoveryNote, PhaseState, Dimension/Phase enums, 41 default ConceptTypes
- [spec] Revised Spec_DataPersistence.md to v0.2 — schema v2 migration, Discovery entity persistence
- [spec] Revised Spec_Navigation.md to v0.2 — three routes, phase transitions, dimension switching

## 2026-04-27

- [spec] Completed Spec_DataModel.md v0.1 — 7 entities, 35 default ConceptTypes, ID strategy
- [spec] Completed Spec_DataPersistence.md v0.1 — atomic writes, save queue, project-as-folder
- [spec] Completed Spec_Navigation.md v0.1 — two routes, Builder switching, preferences
- [spec] Completed Structure_Map.md v0.1 — 7 feature folders, 11 Level 2 specs, writing order
- [spec] Completed Templates_SpecDocs.md v0.1 — Tech Spec, Design Spec, Buildable Unit templates
- [spec] Completed Spec_Story_Engine_PRD.md v0.2 — all open questions resolved
- [docs] Created BUILD_STATUS.md, HARD_RULES.md, OVERVIEW.md
